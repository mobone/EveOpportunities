# Makefile for gopy pkg generation of python bindings to parser.go
# File is generated by gopy (will not be overwritten though)
# gopy.exe pkg .\parser.go

PYTHON=python
PIP=$(PYTHON) -m pip

all: gen

install: install-pkg install-exe

gen:
	gopy.exe pkg -no-make .\parser.go

build:
	$(MAKE) -C parser.go build

install-pkg:
	# this does a local install of the package, building the sdist and then directly installing it
	rm -rf dist build */*.egg-info *.egg-info
	$(PYTHON) setup.py sdist
	$(PIP) install dist/*.tar.gz

install-exe:
	# install executable into /usr/local/bin
	cp parser.go/pyparser.go /usr/local/bin/

